<ContentDialog
    x:Class="MyShop.App.Views.ConfigScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Server Configuration"
    PrimaryButtonText="Save"
    SecondaryButtonText="Cancel"
    CloseButtonText="Close"
    DefaultButton="Primary"
    PrimaryButtonCommand="{x:Bind ViewModel.SaveCommand}"
    Style="{StaticResource DefaultContentDialogStyle}">

    <StackPanel Spacing="15" Width="400">
        <TextBlock Text="Set up your connection to the MyShop backend." 
                   TextWrapping="Wrap" 
                   Style="{StaticResource CaptionTextBlockStyle}" />

        <TextBox Header="GraphQL Endpoint URL"
                 PlaceholderText="http://localhost:4000/graphql"
                 Text="{x:Bind ViewModel.ServerUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <TextBox Header="Database Identifier"
                 PlaceholderText="myshop_db"
                 Text="{x:Bind ViewModel.DatabaseName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" 
                   Foreground="Red" 
                   Visibility="{x:Bind ViewModel.HasError, Mode=OneWay}" />
        
        <Button Content="Reset to Default" 
                Command="{x:Bind ViewModel.ResetCommand}" 
                HorizontalAlignment="Right" />
    </StackPanel>
</ContentDialog>
