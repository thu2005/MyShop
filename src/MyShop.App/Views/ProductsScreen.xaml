<Page
    x:Class="MyShop.App.Views.ProductsScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:MyShop.Core.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Product Inventory" Style="{StaticResource TitleTextBlockStyle}"/>
            <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,10,0,0">
                <Button Content="Refresh List" Command="{x:Bind ViewModel.LoadProductsCommand}"/>

                <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" 
                           Foreground="Red" 
                           VerticalAlignment="Center"
                           Visibility="{x:Bind ViewModel.HasError, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}" 
                Padding="12,8" CornerRadius="4,4,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="ID" FontWeight="Bold"/>
                <TextBlock Grid.Column="1" Text="Name" FontWeight="Bold"/>
                <TextBlock Grid.Column="2" Text="SKU" FontWeight="Bold"/>
                <TextBlock Grid.Column="3" Text="Price ($)" FontWeight="Bold" HorizontalAlignment="Right"/>
                <TextBlock Grid.Column="4" Text="Stock" FontWeight="Bold" HorizontalAlignment="Right"/>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <ProgressRing IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" 
                          Width="50" Height="50" Canvas.ZIndex="1"/>

            <ListView ItemsSource="{x:Bind ViewModel.Products, Mode=OneWay}" 
                      SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Product">
                        <Grid Padding="0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="2*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{x:Bind Id}" Foreground="Gray"/>

                            <TextBlock Grid.Column="1" Text="{x:Bind Name}" FontWeight="SemiBold"/>

                            <TextBlock Grid.Column="2" Text="{x:Bind Sku}" Foreground="Gray"/>

                            <TextBlock Grid.Column="3" Text="{x:Bind Price}" HorizontalAlignment="Right"/>

                            <TextBlock Grid.Column="4" Text="{x:Bind Stock}" HorizontalAlignment="Right"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>